apiVersion: v1
kind: Service
metadata:
  name: my-app-load-balancer
  namespace: my-app
spec:
  type: LoadBalancer
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8888
